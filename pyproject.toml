[project]
name = "msteamdev"
version = "0.1.0"
description = "msteam using crewAI"
authors = [{ name = "Amin Basri", email = "noramin@infopro.com.my" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]>=0.126.0,<1.0.0",
    "fastapi",
    "uvicorn",
    "langchain",
    "langchain-community",
    "langchain_ollama",
    "python-dotenv",
    "pydantic",
    "pdpyras",
    "requests",
    "mcp",
    "pytz",
    "pagerduty",
    "pip",
    "setuptools",
    "wheel",
    "mcpadapt",
    "redis",
    "openlit",
    "pytest",
    "pytest-asyncio"
]

[project.scripts]
msteam = "msteamdev.main:run"
run_crew = "msteamdev.main:run"
train = "msteamdev.main:train"
replay = "msteamdev.main:replay"
test = "msteamdev.main:test"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
